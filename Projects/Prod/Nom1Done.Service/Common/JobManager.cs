using System.Linq;

namespace Nom1Done.Common
{
    public static class JobManager
    {
        //public static TaskMgrJob GetPendingJob(int statusId, int stageId, bool isSending)
        //{
        //    TaskMgrJob obj;
        //    using(var ctx=new EnercrossStoreEntities())
        //    {
        //        obj=ctx.TaskMgrJobs.Where(a => a.StatusId == statusId && a.StageId == stageId && a.IsSending == isSending).FirstOrDefault();
        //    }
        //    return obj;
        //}
    }
}
